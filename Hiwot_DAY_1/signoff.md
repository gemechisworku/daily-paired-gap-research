Gap-closure judgment: Closed. I now understand, in a way I did not before, that total `generate()` latency should be decomposed into prefill (best proxied by TTFT) and decode (best proxied by TPOT), and that when prompt/context tokens increase while completion length stays fixed, the dominant cost shift is usually prefill attention work rather than decode speed. I also understand what evidence is sufficient to prove this in my own runs: logging `prompt_tokens`, `completion_tokens`, `ttft_ms`, `tpot_ms`, and `e2e_ms` across controlled prompt-length sweeps, then checking whether TTFT grows much faster than TPOT. That directly answers the profiling and attribution question I had.
